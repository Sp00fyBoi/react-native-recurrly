# Session Checkpoint — 2026-08-09

## 1. Current State

### Context
A full read-only audit of the codebase was performed, then fixes were implemented in phases.
Full audit report + phased plan lives at:
`C:\Users\Spoofyware\.claude\plans\perform-a-comprehensive-read-only-logical-hopper.md`

**Scope decisions made by the user this session:**
- Android + iOS only. Web is **not** a target.
- **No backend for now**, but the data layer must be ready to migrate to **Supabase + Prisma**.
- Local persistence = **expo-sqlite** (chosen so local tables mirror the future Prisma schema 1:1).
- Execution scope was "Critical + correctness" (Phases 1–3), then the user asked for Phase 4.

### Phases 1–3 — COMPLETE AND VERIFIED
`npx tsc --noEmit` → 0 errors. `npx expo lint` → clean. Route table verified clean.

- **C4** `.claude/worktrees/` added to `.gitignore` (line ~14). Expo Router's file scan respects
  `.gitignore`, so the two untracked worktrees were being enrolled as ~40 phantom routes.
  Verified after Metro restart: 10 real routes, 0 phantom.
- **W1** Two dead CSS class references fixed: `sub-bdy`→`sub-body` (SubscriptionCard),
  `list-action-test`→`list-action-text` (ListHeading).
- **C1** `app/_layout.tsx`: `useFonts` error captured (`fontsLoaded || fontError`) so a bad font
  asset can no longer hang the app behind the splash forever. `.catch()` added to both
  SplashScreen calls.
- **C2** Import-time `throw`s removed. `lib/posthog.ts` now warns; missing Clerk key renders a
  `ConfigurationError` screen. Dropped the `!` assertion that made the guard invisible to TS.
- **C3** `try/catch` added to all 8 async auth handlers (sign-in, sign-up, settings sign-out),
  routed into the existing `statusNotice` UI. Fixed `try/finally`-without-`catch` cases.
- **W7** `ErrorBoundary` exported from `app/_layout.tsx` with a retry action.
- **W10 / F9** Sign-out failure surfaced in Settings; `statusNotice` now renders on the sign-up
  form stage.
- **W3** Create-modal submit button stays pressable while invalid so validation messages appear.
- **C5** Android modal keyboard fix ported from the worktree into
  `components/CreateSubscriptionModal.tsx` (`useAndroidKeyboardHeight` hook + `statusBarTranslucent`).
- **F8** Failed code-send now advances to verify stage with a "use Resend code" notice.
- **F10/F11** Removed broken `reset-project` npm script. `tsconfig.json` now excludes
  `app-example` and `.claude` (NOTE: `app-example/` was **excluded, not deleted** — it is still
  on disk; deletion was avoided because it is gitignored and unrecoverable).

### Phase 4 — CODE COMPLETE, STATIC GATES CLEAN, **RUNTIME UNVERIFIED**
Installed `expo-sqlite@~16.0.10` (`npx expo install`) — this also auto-added the `expo-sqlite`
config plugin to `app.json`. Docs were read at
https://docs.expo.dev/versions/v54.0.0/sdk/sqlite/ per AGENTS.md.

**New files:**
- `lib/db/ids.ts` — UUID v4 generator (Math.random based; documented as swappable for
  expo-crypto / Postgres `gen_random_uuid()`). Deliberately avoided installing a 2nd package.
- `lib/db/schema.ts` — snake_case DDL mirroring the future Postgres table, `SubscriptionRow` type,
  row→domain mappers. Icons are stored as `icon_key` and hydrated to a bundled asset on read.
- `lib/db/client.ts` — module-singleton `getDatabase()` with WAL + `PRAGMA user_version` migration
  runner. Does NOT cache a rejected promise.
- `lib/db/meta.ts` — `app_meta` key/value helpers.
- `lib/repositories/types.ts` — `SubscriptionRepository` interface (the Supabase swap point).
- `lib/repositories/sqlite-subscriptions.ts` — the expo-sqlite implementation.
- `lib/repositories/index.ts` — exports the chosen singleton. **Single place to swap in Supabase.**
- `lib/repositories/seed-demo-data.ts` — one-time demo seeding behind
  `SEED_DEMO_DATA_ON_FIRST_RUN`, with an in-flight promise map to survive React's
  double-invoked dev effects.
- `lib/subscription-metrics.ts` — `calculateMonthlyTotal`, `findNextRenewalDate`,
  `selectUpcomingSubscriptions` (W6 derivations).

**Rewritten / modified:**
- `type.d.ts` — added `userId`, `iconKey`, `createdAt`, `updatedAt`, `SubscriptionStatus`,
  `CreateSubscriptionInput`, `UpdateSubscriptionPatch`. Dropped the write-only `frequency` field.
- `constants/data.ts` — `HOME_BALANCE`, `UPCOMING_SUBSCRIPTIONS`, `HOME_SUBSCRIPTIONS` all removed;
  replaced by `DEMO_SUBSCRIPTIONS: CreateSubscriptionInput[]` with relative dates.
- `lib/subscriptions-store.tsx` — fully rewritten: async, user-scoped, `status`/`error` states,
  `refresh`, `addSubscription`, `updateSubscription`, `removeSubscription`, mount guard.
- `app/(tabs)/_layout.tsx` — switched `useAuth`→`useUser`; passes `userId={user.id}` to provider.
- `app/(tabs)/index.tsx` — rewritten: derived balance + upcoming, `ListHeaderComponent` as an
  element (W4), loading/error screens, cancel wiring. Label changed "Balance"→"Monthly spend"
  (copy change, since a monthly recurring total under "Balance" was misleading).
- `app/(tabs)/subscriptions.tsx` — loading/error screens + cancel wiring.
- `components/SubscriptionCard.tsx` — renders the cancel button using the pre-existing
  `onCancelPress`/`isCancelling` props and `.sub-cancel*` CSS (F1/F7).
- `components/CreateSubscriptionModal.tsx` — emits `CreateSubscriptionInput`; added
  `matchIconKey` restricted to `BRAND_ICON_KEYS` so UI glyphs can't win on substrings.

## 2. Next Steps — EXACT STARTING TASK

### DONE — static verification (both gates clean)
- `npx tsc --noEmit` → **exit 0, no errors.**
- `npx expo lint` → initially 1 warning: `'icons' is defined but never used` in
  `components/CreateSubscriptionModal.tsx` (left over when `matchIconKey` switched from
  `Object.keys(icons)` to `BRAND_ICON_KEYS`). **Fixed** — import narrowed to
  `import type { IconKey } from "@/constants/icons"`. Re-ran both → **exit 0, clean.**

### DONE — metrics logic exercised for the first time
`lib/subscription-metrics.ts` was compiled standalone and run under Node with a 19-case harness
(scratchpad: `metrics.test.js` + `build/`). **All 19 passed.** Covered: yearly÷12 normalisation,
case-insensitive billing match, paused/cancelled exclusion, empty-list → 0 not NaN, soonest-future
renewal selection, past-date and invalid-date tolerance, 30-day window boundary (30 in / 31 out),
"renews later today" → 0 days left, and currency pass-through.
This is a throwaway harness in the scratchpad, not a committed test suite.

### BLOCKED — on-device verification
`adb devices` → **no devices attached.** SM_S921B is not connected, so nothing below has been run.
**Phase 4 has still never executed at runtime: the SQLite schema has never been created and no
query has ever run.** Also note expo-sqlite is a new native module with a config plugin, so this
needs a real rebuild (`npx expo run:android`), not just a Metro reload.

When a device is connected, run `npx expo run:android`, then check:
- First launch on a clean install → **4** demo subscriptions, appearing exactly **once** (not 8 —
  verifies the seed race guard in `seed-demo-data.ts`).
- Home header should read **Monthly spend $97.48**, next-renewal date **11 days out**, and exactly
  **3** Upcoming cards: Adobe 11d, GitHub Pro 15d, Canva Pro 24d.
  (Claude Pro is seeded as `paused`, so it must be absent from both the total and Upcoming — this
  is the visible proof the status filter works. $97.48 = 77.49 + 9.99 + 119.99/12.)
- Create a subscription → force-quit → relaunch → still listed (verifies persistence).
- Expand a card → "Cancel subscription" → flips to Cancelled, survives restart, and drops out of
  the Monthly spend total.
- Open create modal, focus **Price** → keyboard must not cover the field (C5, still unverified).
- Airplane mode → tap "Sign in" → visible error message, not silence (C3, still unverified).

## 3. Worktrees, Temp Files, Pending Commands

- **No background processes are running.** Metro was started on port 8083 to regenerate route
  types and was explicitly stopped (`TaskStop` on task `bxwh1yfza`).
- **Nothing has been committed.** All work is uncommitted working-tree changes on branch `dev`.
- **Worktrees still on disk** at `.claude/worktrees/android-keyboard-modal-fix/` and
  `.claude/worktrees/sprightly-growing-eagle/`. They are now gitignored (required — see below) but
  were not deleted. The keyboard fix has already been ported out of the first one, so it can be
  removed when convenient.
- **CRITICAL, do not undo:** `.claude/worktrees/` must stay in `.gitignore`. Expo Router's file
  scan follows `.gitignore`; un-ignoring it re-enrolls both worktree copies as real, deep-linkable
  routes. Verify with:
  `grep -c "worktrees\|/\.\./" .expo/types/router.d.ts`  → must print `0`.
- `app-example/` (67 KB dead Expo scaffold) is still on disk, excluded via `tsconfig.json`.
  Safe to delete but it is gitignored, so deletion is unrecoverable — ask before removing.
- `PLAN.md` is untracked at the repo root. **It was not created by this session** — leave it alone.
- This checkpoint file `.claude-checkpoint.md` is untracked and not gitignored.
- Memory written to
  `C:\Users\Spoofyware\.claude\projects\C--Users-Spoofyware-Documents-Code-react-native-recurrly\memory\`
  (`MEMORY.md`, `recurrly-scope-and-stack.md`, `recurrly-worktrees-break-expo-router.md`).

### Still open (deliberately deferred, not part of Phases 1–4)
- **W2** — "View all" button in `components/ListHeading.tsx` still has no `onPress`.
- **W8** — `app/(tabs)/insights.tsx` is still a stub rendering the literal text "Insights".
- **W9** — `app/onboarding.tsx` and `app/subscriptions/[id].tsx` are still unreachable, unstyled
  placeholders (no `SafeAreaView`; `[id].tsx` has a misspelled component name).
- **F2** no backend, **F3** no forgot-password, **F4** no OAuth, **F6** currency still hardcoded
  to USD in the create modal, **F5** icon is auto-matched but there is still no icon picker.
- **F12** ~10 unused dependencies still installed (verify against native config before pruning;
  reanimated/gesture-handler may be required transitively).
